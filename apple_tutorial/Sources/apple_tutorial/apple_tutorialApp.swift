import SwiftUI

@main  // App entry point
struct apple_tutorialApp: App {  // structure that implements App protocol
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
